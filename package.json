{
  "name": "boxing-timer",
  "version": "1.0.0",
  "description": "Boxing round timer app",
  "scripts": {
    "build:ios": "npx cap add ios && npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/camera": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
